import { DisplaycommentsPipe } from './displaycomments.pipe';

describe('DisplaycommentsPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplaycommentsPipe();
    expect(pipe).toBeTruthy();
  });
});
