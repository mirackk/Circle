import { DisplaynewsPipe } from './displaynews.pipe';

describe('DisplaynewsPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplaynewsPipe();
    expect(pipe).toBeTruthy();
  });
});
